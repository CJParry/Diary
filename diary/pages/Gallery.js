import React from 'react';
import { SafeAreaView, Text } from 'react-native';

// This page displays the locally saved content files
const Gallery = () => {
  return (
    <SafeAreaView>
        <Text>Gallery of content</Text>
    </SafeAreaView>
  );
};

export default Gallery; 